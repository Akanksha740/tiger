spring:
  application:
    name: tiger
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/tiger}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
  flyway:
    enabled: true
  jackson:
    default-property-inclusion: non_null

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

tiger:
  polymarket:
    gamma-base-url: ${TIGER_POLYMARKET_GAMMA_BASE_URL:https://gamma-api.polymarket.com}
    clob-base-url: ${TIGER_POLYMARKET_CLOB_BASE_URL:https://clob.polymarket.com}
    page-limit: ${TIGER_POLYMARKET_PAGE_LIMIT:100}
  kalshi:
    env: ${KALSHI_ENV:demo}
    key-id: ${KALSHI_KEY_ID:}
    private-key-path: ${KALSHI_PRIVATE_KEY_PATH:}
    api-prefix: /trade-api/v2
    max-retries: 5
  ingestion:
    polymarket-events:
      enabled: false
      limit: ${TIGER_POLYMARKET_PAGE_LIMIT:100}
      offset: 0
    polymarket-catalog:
      enabled: false
      page-limit: ${TIGER_POLYMARKET_PAGE_LIMIT:100}
      start-offset: 0
      # 0 means continue until Gamma returns fewer than page-limit events
      max-pages: 0
    polymarket-orderbook-snapshots:
      enabled: false
      scheduler-enabled: false
      limit: ${TIGER_POLYMARKET_ORDERBOOK_SNAPSHOT_LIMIT:100}
      fixed-delay-ms: ${TIGER_POLYMARKET_ORDERBOOK_SNAPSHOT_FIXED_DELAY_MS:60000}
    kalshi-series:
      enabled: false
      # min-updated-ts:  # optional epoch seconds for incremental /series
    kalshi-events:
      enabled: false
      # Keep low with with-nested-markets=true (large JSON payloads per page)
      page-limit: 50
      with-nested-markets: true
      # When true, poll GET /events?min_updated_ts=... instead of a full catalog scan
      incremental: false
      # min-updated-ts:  # optional epoch seconds; defaults to ingestion_state watermark
    kalshi-open-markets:
      enabled: false
      page-limit: 200
    kalshi-catalog:
      enabled: false
      refresh-series: false
      # When true, incremental series + events-for-new-series + events min_updated_ts + open markets
      incremental: false
    exit-on-complete: false
